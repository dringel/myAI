import { AI_NAME, OWNER_NAME } from "@/configuration/identity";

export const CHAT_HEADER: string = `${AI_NAME} - ${OWNER_NAME} Research Assistant`;
export const MESSAGE_PLACEHOLDER: string = `Ask about our research, publications, or lab members...`;
export const FOOTER_MESSAGE: string = `${AI_NAME} provides information about ${OWNER_NAME}'s research from the past 3 years. For older publications or specific inquiries, please contact the lab directly.`;
export const CLEAR_BUTTON_TEXT: string = `Clear Chat`;
export const PAGE_TITLE: string = `${OWNER_NAME} Research Lab Assistant`;
export const PAGE_DESCRIPTION: string = `Chat with ${AI_NAME}, ${OWNER_NAME}'s AI assistant.`;

export const EMPTY_CITATION_MESSAGE: string = "Unspecified source";
